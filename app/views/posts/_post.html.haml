%tr
  %td= post.title
  %td= post.description
  %td= link_to 'Show', post_path(post)
  %td= link_to 'Edit', edit_post_path(post)
  %td= link_to 'Destroy', post_path(post), method: :delete, remote: true, data: { controller: "posts", action: "posts#remove" }
